from pathlib import Path

from test_static.utils import HandleTestReader


def test_{{ READER_NAME_CC }}():
    test_file = Path(__file__).parent.parent / '{{ TEST_FILE }}'
    with HandleTestReader(test_file.__str__(), '{{ READER_NAME_SC }}_{{UUID}}') as reader:
        assert reader.as_dict['tables'] == []
